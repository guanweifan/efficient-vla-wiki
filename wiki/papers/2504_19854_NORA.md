# 2504_19854_NORA

## Source
- Raw: [[raw/2504_19854_NORA.pdf]]
- Extracts manifest: [[extracts/parses/2504_19854_NORA/manifest.json]]
- Primary text fallback: [[extracts/parses/2504_19854_NORA/pdftotext.txt]]

## Claim
- 这篇论文要解决的是：现有 VLA 往往接近或超过 `7B` 参数，推理和微调成本高，难以在消费级 GPU 上做高效适配；同时视觉编码不足会在抓取等任务上造成失败。
- 作者提出 `NORA`，一个基于 `Qwen-2.5-VL-3B` 的 `3B` 参数 VLA，目标是在更小模型规模下保留较强的视觉语义理解和任务执行能力。
- NORA 采用 `FAST+ tokenizer` 做动作序列离散化，并强调“简单设计也能在真实场景超过大模型/专门空间模型 baseline”，尤其不依赖 spatial grid 或额外 spatial embedding。
- headline numeric claims 包括：
  - backbone 为 `Qwen-2.5-VL-3B`，模型规模 `3B`。
  - 预训练使用 `970k` real-world robot demonstrations。
  - 推理时约使用 `8.3GB` GPU memory。
  - 在 WidowX 真实任务上平均成功率 `56.7%`，高于 `OpenVLA` 的 `40%` 和 `SpatialVLA` 的 `11.1%`。
  - 在 LIBERO 上，`NORA-Long-fine-tuned` 平均成功率达到 `87.9%`，是文中最佳结果。

## Methodology Index
- small open-source generalist VLA
- Qwen-2.5-VL-3B backbone
- FAST+ tokenizer
- discrete action tokens
- autoregressive action prediction
- single-frame observation
- action chunk size = 1
- NORA-Long / action chunk size = 5
- Open X-Embodiment pretraining
- real-world WidowX evaluation
- LIBERO finetuning
- comparison to OpenVLA / SpatialVLA / TraceVLA / RT-1
- action chunking analysis

## Data Pointer
- `PDF p.1` Abstract：
  - NORA 的问题设定、`3B`、`Qwen-2.5-VL-3B`、`970k demonstrations`、`FAST+ tokenizer`、以及“超过大模型 baseline”的 headline 都在这里。
- `PDF p.4-5` Sec. 3 + Fig. 1：
  - NORA 主架构、action token 生成方式、单帧输入、chunk size = 1 的核心设定在这里。
- `PDF p.6-8` Fig. 3 + Table 1 + Fig. 4：
  - WidowX 真实任务主结果；`Average 56.7`、各类任务（multiple objects / OOD object / spatial）的拆分结果都在这里。
- `PDF p.8-9` Table 2：
  - LIBERO 模拟环境结果，尤其是 `NORA-Long-fine-tuned = 87.9 average` 和 `LIBERO-Long = 74.6` 的核心证据在这里。
- `PDF p.9` Fig. 5 / Fig. 6 附近：
  - distraction 设置下性能下降，以及 action chunking 在 WidowX 上表现不稳定的证据入口在这里。
- `PDF p.9-10` Sec. 4.3 / 4.4：
  - 为什么 `NORA-Long` 在模拟里更强、但在 WidowX 上更脆弱；后续若做 action chunking 条件边界，需要回这里补证。

## 待核点
- 论文 abstract 说 NORA “outperforms existing large-scale VLA models”，但主文真实任务表中只直接对比了 `RT-1`、`OpenVLA`、`SpatialVLA`；后续若写更宽泛结论，需要明确比较范围。
- `NORA-Long` 是文中最佳 LIBERO 结果，但不等于 `NORA` 主模型在所有场景都更强；真实 WidowX 上作者明确写了 `NORA-Long` 更不稳，不能把两者混为一体。
- 真实场景里 NORA 在 multi-object tasks 上虽然最好，但成功率仍多在 `30-40%` 区间，后续如写“robust real-world performance”，需要保留这个 caveat。
- `8.3GB` GPU memory 是推理时的资源描述，不是端到端部署成本结论；若后续要和其他模型做 efficiency 比较，还需补更细的速度/硬件口径。
