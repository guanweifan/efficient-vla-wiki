# 2601_05248_LaST0

## Source
- Raw: [[raw/2601_05248_LaST0.pdf]]
- Extracts manifest: [[extracts/parses/2601_05248_LaST0/manifest.json]]
- Primary text fallback: [[extracts/parses/2601_05248_LaST0/pdftotext.txt]]

## Claim
- 这篇论文提出 **LaST0**，目标是在机器人操作中实现更高效的 **reason-before-act**，但不再显式生成冗长的语言 CoT 或未来图像，而是把推理压到一个 **Latent Spatio-Temporal CoT** 空间里。作者的核心判断是：显式 CoT 会带来显著推理延迟，而且语言空间本身不足以充分表达机器人操作中的细粒度物理与几何动态。来源：[[raw/2601_05248_LaST0.pdf]]，第 1-2 页 Abstract、Introduction、Fig. 1。
- 方法上的主张由两部分组成：一是一个 **token-efficient latent CoT space**，用于建模未来 2D 图像、3D 结构和机器人本体感知状态，从而形成时序一致的隐式推理轨迹；二是一个通过 **Mixture-of-Transformers (MoT)** 实现的 **dual-system architecture**，其中 `slow reasoning expert` 低频更新 latent CoT，`fast acting expert` 高频生成动作。来源：[[raw/2601_05248_LaST0.pdf]]，第 1-3 页 Abstract、Introduction；第 4-6 页 Fig. 2、Sec. 3。
- 论文保留了多组 headline numeric claims：在 **10 个模拟任务** 上提升约 **8%**，在 **三类真实世界场景** 上分别提升约 **13% / 14% / 14%**，并且相对先前显式 CoT VLA 方法获得约 **14× speedup**。这些数字有信息量，但它们跨越 simulation / real-world / different prior SOTA baselines，不适合在 `L1` 被压成单一无条件 superiority claim。来源：[[raw/2601_05248_LaST0.pdf]]，第 1 页 Abstract；第 2 页贡献段；第 6-8 页 Table 1、Table 2、Fig. 4。
- 从当前证据看，LaST0 更像一篇**latent CoT + fast/slow dual-system VLA** 论文，而不是简单的推理加速论文。其关键价值在于把“推理更快”与“推理仍能表达物理世界难以语言化的动态”绑定起来，并通过 heterogeneous fast-slow operating ratios 支持部署时的频率切换。来源：[[raw/2601_05248_LaST0.pdf]]，第 1-3 页 Abstract、Introduction；第 4-6 页 Sec. 3。

## Methodology Index
- latent CoT
- Latent Spatio-Temporal Chain-of-Thought
- token-efficient latent reasoning
- dual-system VLA
- Mixture-of-Transformers
- MoT
- slow reasoning expert
- fast acting expert
- heterogeneous fast-slow frequencies
- latent supervision
- future visual latent
- 3D geometric latent
- proprioceptive latent
- Janus-Pro initialization
- Open-X-Embodiment pretraining
- reason-before-act

## Data Pointer
- **Abstract**：第 1 页。适合后续回收 `latent CoT + dual-system + 13/14/14% + 14×` 的 headline。
- **Figure 1 + Introduction**：第 1-2 页。适合后续补 explicit CoT vs latent CoT、reasoning latency 与 “difficult to verbalize” 的方法动机。
- **Figure 2 + Sec. 3**：第 4-6 页。适合后续补 latent CoT space、MoT 双专家、fast/slow 交互和 heterogeneous frequency 训练机制。
- **Table 1 + Fig. 4**：第 6-8 页。适合后续补 RLBench / simulation 结果，以及 `15.4 Hz` 与显式 CoT 方法 / π0.5 的推理频率比较。
- **Table 2**：第 8 页后。适合后续补真实世界 single-arm / dual-arm / mobile manipulation 的 success-rate 对比。
- **Appendix / later figures**：适合后续补 long-horizon real-world rollout、failure cases、以及 latent CoT cache 的实现细节。

## 待核点
- `8%`、`13% / 14% / 14%`、`14× speedup`、`15.4 Hz` 等数字来自不同评测层面；后续需要拆清对应的 baseline、任务集合与比较口径。
- 论文同时用 `LaST0`、`latent CoT`、`dual-system MoT` 三套 framing 讲方法；后续 taxonomy 需要统一它的主定位。
- `14× speedup over previous explicit CoT VLA approaches` 目前仍偏 headline 表述；后续需要明确具体比较对象以及 speedup 是如何测得。
- 论文覆盖 simulation、real-world、long-horizon real-world demo、多机器人平台；后续需要决定哪些属于主 claim，哪些降为 supporting evidence。
