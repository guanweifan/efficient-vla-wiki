# 2602_03983_SD-VLA

## Source
- Raw: [[raw/2602_03983_SD-VLA.pdf]]
- Extracts manifest: [[extracts/parses/2602_03983_SD-VLA/manifest.json]]
- Primary text fallback: [[extracts/parses/2602_03983_SD-VLA/pdftotext.txt]]

## Claim
- 页面定位：这是一篇 **long-horizon context + efficient inference via token disentanglement** 论文；它的核心贡献是把视觉输入拆成 static/dynamic token 并联动 KV-cache 复用，而不是一般的模型压缩。
- 这篇论文要解决的是：VLA 处理多帧历史时会遇到两类问题，一是多帧拼接导致 context 迅速膨胀，二是每一步都重算全部视觉 token 的 KV cache 会造成很高的推理开销；因此现有 VLA 很难既保留长时记忆，又保持推理效率。
- 作者提出 `SD-VLA`，核心是 `static-dynamic disentanglement`：把视觉 token 拆成具有不同时间持久性的 static tokens 与 dynamic tokens。static tokens 跨多个 timestep 只保留一份，dynamic tokens 才随时间拼接；同时通过 trainable `recache gate` 决定 static tokens 何时复用、何时刷新，从而把长时记忆建模与 KV-cache 复用统一起来。
- 论文主张：VLA 轨迹中的大量视觉信息在时间上是静态或缓慢变化的，若显式建模这种 temporal persistence，就能同时解决 long-horizon temporal context 与 efficient inference 两个瓶颈。
- headline 数字需要拆开理解：
  - `39.8%` 对应作者提出的 memory-dependent benchmark 上的绝对成功率提升；
  - `4.9% + 2.26×` 对应 `SimplerEnv` 上的 success-rate gain 与 inference speedup；
  - `0.7% + 1.70×` 对应 `LIBERO` 上的 success-rate gain 与 speedup。  
- 更稳的主张是：`SD-VLA` 通过 static-dynamic disentanglement 与 recache gate，把长时记忆建模和推理加速绑在同一套 temporal persistence 假设上；它不是无条件的通用 speedup，而是建立在“场景中存在可复用静态信息”这一前提之上的方法。

## Methodology Index
- static-dynamic disentanglement
- SD-VLA
- long-horizon VLA
- temporal persistence
- static tokens
- dynamic tokens
- multi-level static cache
- KV-cache reuse
- recache gate
- multi-frame integration
- LIBERO-memory benchmark
- SimplerEnv acceleration
- context length reduction

## Data Pointer
- `PDF p.1-2` Abstract + Fig. 1：
  - long-horizon context 与 inference efficiency 的双重问题、static-dynamic disentanglement 的核心直觉、以及 `39.8%` / `2.26×` / `1.70×` 的 headline 都在这里。
- `PDF p.3-5` Sec. 3 + Fig. 3：
  - static tokens / dynamic tokens、多级 static cache、recache gate 与训练目标都在这里。
- `PDF p.5-6` Table 1：
  - 作者提出的 memory-dependent benchmark 主结果在这里；`On Stove`、`Position Reset`、`Doneness` 三个指标都在这里。
- `PDF p.6-7` Table 2：
  - SimplerEnv 上与 base CogACT、FlashVLA、TTF、VLA-Cache 的比较在这里；`2.26×` speedup 与 `92.4` / `38.9` 等主结果应回这张表。
- `PDF p.1-2` Introduction + `PDF p.7+` result discussion：
  - `LIBERO` 上 `+0.7%` 与 `1.70×` speedup 的摘要式结果在引言和结果总结里有明确说明，后续若要补更细数值应继续回表格/附录核定。
- `PDF p.5-6` benchmark design section + Fig. 4：
  - `LIBERO-memory` 的任务设计和 episodic-memory 风格评估目标在这里，适合后续补“为什么这比旧 benchmark 更能测 temporal dependency”的证据。

## Evidence Links
- [[wiki/evidence/claims/2602_03983_SD-VLA-headline-split.md|2602_03983_SD-VLA-headline-split]]

## 待核点
- `39.8% absolute improvement` 来自作者新建的 memory-dependent benchmark，而不是 SimplerEnv / LIBERO；后续引用时要明确 benchmark 范围。
- `2.26×` 与 `1.70×` 分别对应 SimplerEnv 与 LIBERO，不应混成一个统一 speedup 数值。
- 当前主文里最容易直接抓到的是 SimplerEnv 与 memory benchmark 的表格结果；LIBERO 的更细指标若后续需要精确数值，可能还要继续回表格或附录核定。
- SD-VLA 的收益建立在 static token 可复用这一任务结构假设上；若后续推广到视觉变化极快、场景静态成分很弱的任务，需要保留适用范围 caveat。
- 若后续把它并入一般 `KV cache reuse` 路线，需要保留“核心是 token disentanglement + temporal persistence，而不是普通 cache engineering”这一边界。
