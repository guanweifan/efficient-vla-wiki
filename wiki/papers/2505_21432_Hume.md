# 2505_21432_Hume

## Source
- Raw: [[raw/2505_21432_Hume.pdf]]
- Extracts manifest: [[extracts/parses/2505_21432_Hume/manifest.json]]
- Primary text fallback: [[extracts/parses/2505_21432_Hume/pdftotext.txt]]
- Fine-grained locator: [[extracts/parses/2505_21432_Hume/pdftotext.bbox.html]]

## Claim
- 页面定位：这是一篇 **dual-system VLA / asynchronous slow-thinking + fast-refinement control** 论文；核心贡献是把低频 value-guided deliberation 与高频 action denoising 执行解耦，而不是一般性的“更快更强 VLA”报告。
- 这篇论文提出 **Hume**，把它明确定位成一个 **dual-system Vision-Language-Action model**：`System 2` 负责低频、value-guided 的“慢思考”，`System 1` 负责高频、反应式的动作细化与执行。
- 作者的核心方法命题是：VLA 的复杂控制能力可以通过两段式协作显著增强，而不是只靠单一高频或单一低频策略。具体来说，`System 2` 通过 **repeat sampling + value-query head + best-of-N selection** 进行价值引导的候选动作选择，`System 1` 再对被选中的长时域动作块做 **cascaded action denoising**，以兼顾 deliberation 和实时控制。
- 论文给出的 headline 能力叙述需要拆开理解：`System 2` 低频运行约 **4 Hz**、`System 1` 异步高频运行约 **90 Hz**，这是系统运行频率，不等同于 benchmark 结果；性能 headline 则分别来自 `LIBERO`、`Simpler` 和 real-world deployment，不应压成单一“统一 SOTA”结论。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 1-2 页，Abstract / Introduction。
- 作者在引言中给出三组 headline numeric claims：相对 `π0`，Hume 在 `LIBERO` 上约 **+4.4%** success rate、在 `Simpler` 上约 **+25.9%**、在 real-world deployments 上约 **+12.9%**。更稳的写法是：Hume 在不同 evaluation layer 上都报告了提升，但这些数字分别绑定不同 benchmark、platform 与 comparison protocol。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 2 页，Introduction 结尾贡献列表。  
  caveat：这些 headline 数字聚合了不同平台和评测口径，后续 `L2` 需要拆到具体表与具体 setting。

## Methodology Index
- dual-system VLA
- System-2 slow thinking
- value-guided thinking
- value-query head / state-action value estimation
- best-of-N action candidate selection
- flow matching action denoising head
- cascaded dual-system action denoising
- asynchronous low-frequency System 2 + high-frequency System 1
- long-horizon action chunk selection and short-horizon refinement

## Data Pointer
- **Abstract + Intro**：论文整体问题设定、dual-system framing、以及最紧凑的 headline 主张。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 1-2 页。
- **Method core**：`Sec. 3 Methodology`，尤其 `Figure 2`，是后续补 `System 2 / value-query / best-of-N / cascaded denoising` 结构证据的主入口。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 3-4 页，Sec. 3 / Fig. 2。
- **Main LIBERO result**：`Table 1`，后续如果要拆 `LIBERO-Spatial/Object/Goal/Long` 的具体增益，这里是主表。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 8 页，Table 1。
- **Main Simpler result**：`Table 2`，后续如果要拆 WidowX / Google Robot 的 Simpler 指标，这里是主表。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 8 页，Table 2。
- **Real-world result entry**：`Figure 5` 与随后的 WidowX real-world 结果段，是后续补 “failure recovery / zero-shot robustness / real-world average success” 的入口。  
  来源：[[raw/2505_21432_Hume.pdf]]，第 8 页，Fig. 5 与 Sec. 4.2。

## 待核点
- 引言中的 `+25.9%` 与 `+12.9%` headline 数字需要和正文表格逐项对齐；当前页面只保留为作者 headline claim，不视为已完全钉实的结构化证据。
- 论文同时汇报 `LIBERO`、`SimplerEnv`、`WidowX`、`Franka`、`AgiBot G-1` 等多平台结果；后续如果进入 `L2`，需要决定是按 benchmark 拆 evidence，还是按 “simulation / real-world” 两层拆。
- “state-of-the-art” 的表述覆盖了多个不同 comparison set；后续 chief editor 可能需要决定是否把 `OpenVLA-OFT`、`π0`、`GR00T` 等主要对手单独列为 comparison anchor，并把 `4 Hz / 90 Hz` 的系统频率与 benchmark headline 明确分栏。
