# 2506_12723_SP-VLA

## Source
- Raw: [[raw/2506_12723_SP-VLA.pdf]]
- Extracts manifest: [[extracts/parses/2506_12723_SP-VLA/manifest.json]]
- Primary text fallback: [[extracts/parses/2506_12723_SP-VLA/pdftotext.txt]]
- Fine-grained locator: [[extracts/parses/2506_12723_SP-VLA/pdftotext.bbox.html]]

## Claim
- 页面定位：这是一篇 **joint model scheduling + token pruning** 论文；核心贡献是把 temporal redundancy 和 spatial redundancy 统一建模，而不是单独的 pruning 或单独的 action scheduler。
- 这篇论文要解决的是：现有 VLA 加速方法多把推理视为单步静态视觉-动作映射，主要做结构裁剪或 token 压缩，但没有显式处理 sequential decision-making 中的时间冗余，因此在执行频率和实时性上仍然受限。
- 核心主张是：VLA 推理中同时存在时间冗余和空间冗余，因此可以把“模型调度”和“token 剪枝”联合起来做，而不是把它们分开处理。具体来说，先区分 `deliberative` 与 `intuitive` 动作，在高精度 VLA 与轻量 action generator 之间做协同调度；再对被调用的 VLA 分支进行 `spatio-semantic dual-aware` token pruning。
- 作者提出 `SP-VLA`，并声称在 `LIBERO` 上可实现 `1.5× lossless acceleration`，在 `SimplerEnv` 上可达 `2.4×` 加速并获得最高 `6%` 的平均性能提升；同时在 RTX 4090 上，推理频率/时延相对基线分别提升 `1.4×`（LIBERO）与 `2.2×`（SimplerEnv）。这些 headline 来自不同 benchmark、不同 speedup 口径与不同 operating point，更稳的写法是：**SP-VLA 通过联合调度和 dual-aware pruning，在 LIBERO 与 SimplerEnv 上取得一组 benchmark-dependent 的速度-性能 trade-off。** 来源：[[raw/2506_12723_SP-VLA.pdf]]，第 1 页摘要；第 7-9 页 Tables 1、2、4。

## Methodology Index
- joint model scheduling and token pruning
- temporal redundancy reduction
- spatial redundancy reduction
- action-aware model scheduling
- deliberative vs intuitive actions
- lightweight action generator
- Ridge Regression
- action buffer / action cache
- spatio-semantic dual-aware pruning
- Canny edge information
- semantic importance via attention
- speed-aware threshold
- frequency-adaptive inference
- OpenVLA
- CogACT
- LIBERO
- SimplerEnv

## Data Pointer
- 摘要与总体问题设定：[[raw/2506_12723_SP-VLA.pdf]] 第 1 页摘要。这里给出联合调度 + 剪枝的总命题，以及 `1.5× / 2.4× / 6% / 2.2× / 1.4×` 的 headline 结果。
- 主思路概览：[[raw/2506_12723_SP-VLA.pdf]] 第 2 页 Fig. 1。这里展示 `intuitive` 动作走轻量 generator、`deliberative` 动作走 VLA、且 VLA 分支再做 token pruning 的整体流程。
- 方法细节：[[raw/2506_12723_SP-VLA.pdf]] 第 5 页 Fig. 3 与相邻方法章节。这里定义 action-aware scheduling、Ridge Regression action generator、以及 `spatio-semantic dual-aware` pruning 的细节。
- 主结果：[[raw/2506_12723_SP-VLA.pdf]] 第 7-8 页 Table 1 与 Table 2。这里分别对应 `LIBERO` 与 `SimplerEnv` 的主实验结果，也是 `lossless acceleration`、`2.4×` 和 `6%` 等结论的核心证据位置。
- 延迟与频率：[[raw/2506_12723_SP-VLA.pdf]] 第 9 页 Table 4。这里是 RTX 4090 上的 latency / frequency 指标，支撑 `2.2×` 与 `1.4×` 的主表述。

## 待核点
- 论文同时使用了多种 acceleration 口径：`1.5× lossless acceleration`、`1.5× with <3% drop`、`2.4× in SimplerEnv`、`2.2× latency/frequency improvement`、`1.4× in LIBERO`；后续需要固定主口径，避免把不同设定混成同一句 headline。
- `lossless acceleration` 更准确地说是 `LIBERO` 上某组配置的结果；论文自己也给出 `1.5×` 但伴随约 `3%` 精度下降的配置，因此后续不能把“1.5×”一概表述成无损。
- `SimplerEnv` 主结果里正文有 `2× speedup` 的总结性表述，而摘要/结论与表格上限又写到 `2.4×`；后续需要明确默认是写“最高可达 2.4×”还是“主结果约 2×-2.4×”。
- 论文的强项是“联合处理 temporal + spatial redundancy”的统一框架，但 headline 容易被 benchmark 数字主导；后续 chief editor 需要决定 paper-level 页面是以方法命题为中心，还是以 benchmark headline 为中心。
