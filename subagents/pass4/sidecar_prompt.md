只做只读辅助，不得写正式 `wiki/` 页面。

任务范围：
1. 找 `raw` 中与指定主题相关的页码、table / figure 编号、caption 和局部 wording。
2. 指出哪些论文属于同一比较轴，哪些论文不应直接比较。
3. 回传锚点、局部原文和候选比较轴。

禁止事项：
1. 不得创建或修改 `wiki/synthesis/*`。
2. 不得修改 `wiki/papers/*` 或 `wiki/evidence/*`。
3. 不得输出主题结论定稿。
