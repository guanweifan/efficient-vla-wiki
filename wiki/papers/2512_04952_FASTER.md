# 2512_04952_FASTER

## Source
- Raw: [[raw/2512_04952_FASTER.pdf]]
- Extracts manifest: [[extracts/parses/2512_04952_FASTER/manifest.json]]
- Primary text fallback: [[extracts/parses/2512_04952_FASTER/pdftotext.txt]]

## Claim
- 这篇论文要解决的是：autoregressive VLA 虽然在高维连续动作建模上有统一生成优势，但其效率和性能高度受 action tokenizer 质量制约；现有离散 action tokenization 往往要么压缩率不足、推理慢，要么重建质量差、拖累策略性能。
- 核心主张是：如果把 action chunk 重新表示成 single-channel image，并用 neural tokenizer 学习非均匀的动作 patch 分组与离散压缩，再配合 block-wise autoregressive decoding 与 lightweight action expert，就能同时提升 AR-VLA 的动作重建质量、任务成功率和推理效率。
- 作者提出 `FASTER`，包括 `FASTerVQ` tokenizer 与 `FASTerVLA` policy。论文强调：在共享 `PaliGemma` backbone 的实现下，`FASTerVLA` 在 `LIBERO` 上的 latency 为 `112 ms`，低于 `π0` 的 `176 ms` 与 `π0-FAST` 的 `197-556 ms`；在 ablation 中，`BAR` 可把 `LIBERO` success rate 从 `95.5` 提到 `96.7`，同时把 latency 从 `323 ms` 降到 `140 ms`，再结合 `AE` 可达到 `97.7` success rate。来源：[[raw/2512_04952_FASTER.pdf]]，第 1 页摘要；第 7-8 页 efficiency tables；第 11-12 页 ablation。

## Methodology Index
- FASTER
- FASTerVQ
- FASTerVLA
- neural action tokenization
- autoregressive VLA
- single-channel action image
- non-uniform action grouping
- semantically meaningful patches
- hybrid Transformer encoder
- residual vector quantization
- RVQ
- temporal-domain reconstruction
- frequency-domain reconstruction
- block-wise autoregressive decoding
- BAR
- action expert
- AE pretraining
- shared PaliGemma backbone
- LIBERO
- R1Lite-WBC

## Data Pointer
- 摘要与总体框架：[[raw/2512_04952_FASTER.pdf]] 第 1-2 页摘要、引言与 Fig. 1。这里给出“tokenizer + AR policy”的总结构，以及论文为什么把 action tokenization 视为 AR-VLA 的关键瓶颈。
- tokenizer 方法细节：[[raw/2512_04952_FASTER.pdf]] 第 3-5 页 Fig. 2 与方法章节。这里说明 `FASTerVQ` 如何把 action chunk 映射为 single-channel image、做非均匀 patch grouping，并在时域和频域联合重建。
- 推理效率主结果：[[raw/2512_04952_FASTER.pdf]] 第 7-8 页 Table 2 与 Table 5。这里对应 observation encoding、tokenizer latency、以及在共享 backbone 下 `112 ms vs 176 ms vs 197-556 ms` 的 runtime 对比。
- 主性能与 cross-backbone 结果：[[raw/2512_04952_FASTER.pdf]] 第 6-8 页主结果表格与相关正文。这里可回查 `FASTerVQ` 对不同 backbone 的影响，尤其是将 `InternVL3.5-2B` 提升 `17.3%` 到 `96.65` 的 tokenizer headline。
- 组件消融：[[raw/2512_04952_FASTER.pdf]] 第 11-12 页 Table 7 与相邻正文。这里说明 `BAR`、`AE pretraining` 与 full `FASTer` 分别带来的 success-latency tradeoff，也是 `95.5 -> 96.7 -> 97.7` 与 `323 ms -> 140 ms` 的主要出处。

## 待核点
- 论文没有一个统一适用于所有 benchmark 的单一 headline；`112 ms`、`17.3%`、`95.5 -> 96.7 -> 97.7` 分别来自不同设置，后续不能压成一个无条件结论。
- `112 ms` 等 runtime 结论依赖共享 `PaliGemma` backbone、PyTorch 实现与 `RTX 5090` 硬件条件，不能直接写成一般性的端到端部署速度。
- 论文明确指出大部分性能提升来自 `FASTerVQ` tokenizer，本体 `BAR + AE` 进一步补充速度和性能；后续需要决定页面主轴是 tokenizer 创新还是完整 `FASTerVLA` 系统。
- `FASTer` 既是 tokenizer paper，也是 autoregressive policy paper；如果后续进入更高层 taxonomy，需要决定它主要归入 tokenization 路线还是 AR decoding / policy design 路线。
