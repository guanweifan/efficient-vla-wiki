# 2602_01166_LaRA-VLA

## Source
- Raw: [[raw/2602_01166_LaRA-VLA.pdf]]
- Extracts manifest: [[extracts/parses/2602_01166_LaRA-VLA/manifest.json]]
- Primary text fallback: [[extracts/parses/2602_01166_LaRA-VLA/pdftotext.txt]]

## Claim
- 这篇论文提出 **LaRA-VLA**，目标是在 VLA 中把显式 textual / visual CoT 内化成**连续 latent reasoning**，从而兼顾 reasoning 结构与实时控制效率。作者的核心判断是：现有 CoT-based VLA 虽然有效，但一方面推理时会生成冗长的显式中间结果，带来很高的 latency；另一方面，无论文字 CoT 还是离散视觉 token CoT，都与机器人连续感知和连续控制存在表征错配。来源：[[raw/2602_01166_LaRA-VLA.pdf]]，第 1-2 页 Abstract、Introduction、Fig. 1。
- 方法上的主张是一个 **unified latent-reasoning VLA**：同时把 textual CoT 和 visual reasoning internalize 到 **continuous latent representations** 中，在 latent space 里完成 reasoning 与 prediction，并在推理阶段完全消除显式 CoT generation。为实现这一点，论文引入了一个 **curriculum-based training paradigm**，逐步从显式 textual/visual CoT 监督过渡到 latent reasoning，再把 latent reasoning dynamics 适配到 action generation。来源：[[raw/2602_01166_LaRA-VLA.pdf]]，第 1 页 Abstract；第 2-5 页 Introduction、Fig. 2、方法章节。
- 论文还强调训练数据与训练稳定性设计：作者构建了 **LIBERO-LaRA** 与 **Bridge-LaRA** 两个结构化 CoT 数据集，并用 **EMA visual encoder** 作为 target network 稳定 latent 表示学习，避免表示坍塌。当前这些内容更像方法能否成立的关键配套，而不是附属工程细节。来源：[[raw/2602_01166_LaRA-VLA.pdf]]，第 2 页贡献段；第 4-6 页方法、数据与训练描述。
- 论文保留了清晰的 headline efficiency/performance claims：相对显式 CoT 方法可将 inference latency 降低 **up to 90%**；在 simulation 与 long-horizon real-robot manipulation 上持续优于 prior SOTA；文中还给出了诸如 **135 ms latency**、以及在不同 benchmark /场景下的更具体提升数字。这些数字有信息量，但它们跨越 latency、simulation benchmark、real-robot success，不应在 `L1` 被压成单一无条件 superiority claim。来源：[[raw/2602_01166_LaRA-VLA.pdf]]，第 1 页 Abstract；第 8-10 页实验结果与总结。

## Methodology Index
- latent reasoning
- latent CoT
- continuous latent representations
- unified reasoning and prediction in latent space
- curriculum-based training
- explicit-to-latent transition
- textual CoT latent
- visual goal latent
- EMA visual encoder
- latent supervision
- LIBERO-LaRA
- Bridge-LaRA
- action-oriented latent reasoning
- no explicit CoT at inference
- simulation + long-horizon real-robot evaluation

## Data Pointer
- **Abstract**：第 1 页。适合后续回收 `up to 90% latency reduction`、latent reasoning 和 curriculum training 的 headline。
- **Figure 1 + Introduction**：第 1-3 页。适合后续补 textual/visual CoT 与 LaRA-VLA 的差异，以及“离散 CoT 与连续控制错配”的方法动机。
- **Figure 2 + training overview**：第 4-6 页。适合后续补三阶段 curriculum、explicit CoT -> latent reasoning -> action adaptation 的训练路径。
- **Dataset / training description**：第 5-7 页。适合后续补 `LIBERO-LaRA`、`Bridge-LaRA`、EMA encoder 与 latent supervision 的作用。
- **Table 2 及实验章节**：第 8-10 页。适合后续补在 LIBERO / SimplerEnv / long-horizon real-robot 上的对比结果，以及具体 latency 数字如 `135 ms`。
- **Later ablations**：后续章节。适合后续补 latent reasoning components、curriculum 替换过程、以及显式 CoT token 退火的影响。

## 待核点
- `up to 90% latency reduction`、`135 ms`、以及 simulation / real-robot 上的性能提升来自不同评测层面；后续需要拆清具体 baseline 和指标口径。
- 论文标题是 `Latent Reasoning VLA`，但仓库里文件名采用 `LaRA-VLA`；后续需要统一命名口径，避免 `LaRA-VLA` 与 `Latent Reasoning VLA` 并存引发混淆。
- LaRA-VLA 同时强调 `latent reasoning paradigm`、`curriculum-based training`、`continuous latent CoT` 和 `action adaptation`；后续 taxonomy 需要统一其主定位。
- 当前方法仍依赖显式 CoT 与视觉预测作为训练阶段 supervision，只是在 inference 阶段去掉显式 CoT；后续需要决定在单篇主 claim 中是否更显式写出这一点，避免误读为完全 teacher-free latent reasoning。
