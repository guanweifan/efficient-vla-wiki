# 2603_09513_VQ-Memory

## Source
- Raw: [[raw/2603_09513_VQ-Memory.pdf]]
- Extracts manifest: [[extracts/parses/2603_09513_VQ-Memory/manifest.json]]
- Primary text fallback: [[extracts/parses/2603_09513_VQ-Memory/pdftotext.txt]]

## Claim
- 这篇论文要解决的是：现有 articulated manipulation benchmark 多聚焦短时、近 Markov 的简单操作，难以覆盖门锁、密码锁、逻辑锁等需要长期阶段记忆与非当前观测推断的任务；同时，直接把原始历史状态当 memory 又容易带来噪声敏感和过拟合。
- 作者提出两部分贡献：一是 `RuleSafe`，用规则化 articulated lock 机制构造 non-Markovian、long-horizon manipulation benchmark；二是 `VQ-Memory`，把历史 joint-state sequence 经 `VQ-VAE` 压缩成离散 memory tokens，以更紧凑、结构化且噪声鲁棒的方式向 VLA / diffusion policy 提供阶段上下文。
- 论文主张：对于需要阶段感知和历史记忆的长时操作任务，离散化的 `VQ-Memory` 比 raw joint-state memory 更稳定、更抗过拟合，并且能以模型无关方式显著提升不同 policy 的 temporal reasoning。
- headline numeric claims 包括：
  - 在 multi-task `RuleSafe` 设置下，`π0` 集成 `VQ-Memory` 后，平均 success rate 从 `25.0%` 提升到 `56.3%`，process score 从 `48.8%` 提升到 `76.5%`。
  - 在困难的 `rule 020` 上，`VQ-Memory` 也能稳定提升 `DP3`、`RDT`、`CogACT` 等不同基座模型的表现，作者据此主张其具有 architecture-agnostic 增益。

## Methodology Index
- RuleSafe benchmark
- articulated locking manipulation
- non-Markovian manipulation
- long-horizon temporal dependency
- VQ-Memory
- VQ-VAE
- discrete memory tokens
- joint-state memory
- temporal context
- task-phase awareness
- overfitting mitigation
- architecture-agnostic memory module
- multi-task long-horizon manipulation

## Data Pointer
- `PDF p.1-2` Abstract + Introduction：
  - RuleSafe 的问题动机、non-Markovian articulated manipulation 的设定、以及 `VQ-Memory` 的高层主张都在这里。
- `PDF p.3-5` Fig. 1 + benchmark construction section：
  - `RuleSafe` 中 part-phase / task-phase 规则、为什么锁任务需要历史记忆、以及 benchmark 设计逻辑都在这里。
- `PDF p.5-6` Sec. 4.2 + Fig. 2 / Fig. 3：
  - `VQ-Memory` 如何从 joint-state sequence 生成离散 memory tokens、为何它比 raw memory 更稳，以及 clustering / token semantics 可视化都在这里。
- `PDF p.6-7` Table 1 + Table 2：
  - single-task 与 cross-model 泛化结果在这里；`rule 020` 上对 `DP3 / RDT / CogACT` 的提升应回这里核定。
- `PDF p.7` Table 3：
  - multi-task 主结果在这里；`25.0% -> 56.3%` 和 `48.8% -> 76.5%` 的 headline 应回这张表。
- `PDF p.8` Table 4：
  - cluster 数量与 memory length 的 ablation 在这里，适合后续补“为什么离散 memory 能更稳”和“最佳 memory budget 在哪里”。

## 待核点
- `25.0% -> 56.3%` 与 `48.8% -> 76.5%` 来自 `π0` 在 `RuleSafe` multi-task setting 下的 headline，不应直接外推到所有 base model。
- `VQ-Memory` 的 architecture-agnostic 主张主要来自 `rule 020` 上对 `DP3 / RDT / CogACT` 的跨模型增益；这更像困难单任务泛化证据，而不是全 benchmark 全 setting 的统一结论。
- 论文当前更像是在证明“高层时序记忆与阶段感知”问题已被显著改善，但作者也明确保留了执行精度仍是瓶颈的 caveat；后续不能把 remaining failures 忽略掉。
- `RuleSafe` 和 `VQ-Memory` 是绑定叙述的双贡献：一个是 benchmark，一个是 memory module；后续若只引用 `VQ-Memory`，需要说明其主验证场景主要来自作者自建 benchmark。
