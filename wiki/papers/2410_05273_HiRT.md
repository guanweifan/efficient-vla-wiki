# 2410_05273_HiRT

## Source
- Raw: [[raw/2410_05273_HiRT.pdf]]
- Extracts manifest: [[extracts/parses/2410_05273_HiRT/manifest.json]]
- Primary text fallback: [[extracts/parses/2410_05273_HiRT/pdftotext.txt]]

## Claim
- 这篇论文聚焦 VLA / VLM 后端带来的低控制频率和高延迟问题，目标是在尽量保留泛化能力的同时，把系统推到更适合动态操作的控制速度。
- 作者提出 `HiRT`，把慢速、高层语义理解的 VLM 与高速、低层动作执行策略做成层级式异步系统：VLM 低频更新 latent，轻量视觉策略高频读取 latent 并连续输出动作。
- 论文主张这种层级拆分能把“大模型的语义理解”和“小模型的实时控制”结合起来，而不是直接让 VLM 端到端输出低层动作。
- headline 结果是：
  - 静态任务里，控制频率提升到原始 VLA 的接近 2 倍，同时性能大体可比。
  - 动态真实场景里，平均成功率从 `48%` 提升到 `75%`。
  - 在文中主结果表里，HiRT 在动态真实任务上的平均成功率最高，同时完成时间最短。

## Methodology Index
- hierarchical interactive imitation learning
- dual-process inspiration / slow System 2 + fast System 1
- VLM backbone: InstructBLIP (7B)
- understanding module + execution module
- latent-conditioned policy
- asynchronous VLM execution
- latent cache / asynchronous sampling
- lightweight visual encoder
- FiLM conditioning
- cross-attention conditioning
- prefix tuning on action head
- LoRA finetuning on VLM branch
- multi-task imitation learning across simulation + real world

## Data Pointer
- `PDF p.1` Abstract：
  - 问题定义、HiRT 总体主张、`double the control frequency`、动态真实任务 `48% -> 75%` 的 headline 结果都在这里。
- `PDF p.2` Fig. 1：
  - HiRT 的高层结构图，最适合后续补 “slow VLM + fast policy” 的系统关系。
- `PDF p.4-5` Sec. 3.2-3.3：
  - latent-conditioned policy 的三个 conditioning 策略（FiLM / cross-attention / prefix tuning）以及异步缓存、训练时的 asynchronous sampling。
- `PDF p.6` Table 1 + Fig. 4：
  - 静态任务的 success rate 与 speed 对照；`9.8 Hz`、接近 Vanilla-VLA 性能但更高推理速度的核心证据在这里。
- `PDF p.7` Table 2 + Fig. 6：
  - 动态真实任务的核心结果；`Time/s`、`Seen/Unseen/Average` 以及高延迟 baseline 失败的可视化对比都在这里。
- `PDF p.7-8` Table 3：
  - 说明 image context 与 combined conditioning 对性能的作用，适合后续补 ablation evidence。

## 待核点
- `double the control frequency` 在正文主表里对应的是 `4.1 Hz -> 9.8 Hz`，更接近 `2.39x`；后续如果要引用“翻倍”这一说法，需区分 abstract headline 与具体实验设置。
- 静态真实任务里，HiRT 的平均值 `70.0` 低于 Vanilla-VLA 的 `71.3`；后续若写“整体优于 baseline”，必须明确区分 simulation、static real-world、dynamic real-world 三类结果。
- 文中同时讨论多种 conditioning 方式和多种视觉输入，后续若要归纳“HiRT 的关键增益来自哪里”，需要进一步回到 Table 3 和 appendix 做更细证据拆分。
- 异步频率与 latent stale 问题在主文只给出机制描述；若后续要把“异步采样”单独做成方法点，需要进一步补 appendix 中关于 asynchronous sampling 的实验细节。
