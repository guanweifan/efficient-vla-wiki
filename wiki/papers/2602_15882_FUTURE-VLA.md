# 2602_15882_FUTURE-VLA

## Source
- Raw: [[raw/2602_15882_FUTURE-VLA.pdf]]
- Extracts manifest: [[extracts/parses/2602_15882_FUTURE-VLA/manifest.json]]
- Primary text fallback: [[extracts/parses/2602_15882_FUTURE-VLA/pdftotext.txt]]

## Claim
- 页面定位：这是一篇 **unified VLA + world model / future forecasting under fixed latency budget** 论文；它的核心贡献是把 long-horizon control 与 future prediction 放进同一高压缩生成框架，并结合 `HIL` 做执行侧闭环，而不是单独提出某个 forecasting module。
- 这篇论文要解决的是：机器人上的 `VLA + world model` 体系在长时序、多视角、实时执行场景下仍存在三重瓶颈，即长历史带来的 visual-token cost 爆炸、预测与控制之间的表征/执行脱节、以及高维未来预测无法跟上闭环执行频率。
- 作者提出 `FUTURE-VLA`，把 long-horizon control 与 future forecasting 重写成单一的 sequence-generation 任务，并采用 `dual-sided efficiency paradigm`：输入侧做受 token budget 约束的时空压缩，输出侧在紧凑 latent space 中做 future autoregression，从而在单次 forward pass 中同步生成 action chunks 与 future previews。
- 论文主张：如果把历史建模、动作生成和未来预测统一到一个高压缩、实时可执行的 VLA 框架里，就能在不增加单帧基线延迟的前提下，把时空窗口扩到 `16×`，同时显著提升 long-horizon benchmark 与 real-world 执行效果。
- headline 数字需要拆开理解：
  - `99.2%` 对应 `LIBERO`；
  - `75.4%` 对应 `RoboTwin`；
  - `78.0%` 对应 real-world `Piper` platform；
  - `16×` 则是“在维持 single-frame baseline latency 前提下扩展 spatiotemporal window”的架构级主张，而不是任务成功率口径。
- 更稳的主张是：`FUTURE-VLA` 证明通过输入侧时空压缩、latent future autoregression 与 prediction-guided `HIL` 的组合，可以在固定延迟预算下把更长历史、更远未来和闭环执行统一进同一套 VLA 框架，但其 headline 成果需要始终保留 benchmark 与 `HIL` 依赖边界。

## Methodology Index
- FUTURE-VLA
- unified VLA + world model
- dual-sided efficiency paradigm
- constrained-budget spatiotemporal compression
- temporally adaptive cascaded compression
- latent-space autoregression
- future forecasting
- FAST spectral action tokenization
- compact 1D visual tokenization
- prediction-guided HIL
- execution gating
- single forward pass for action chunks and future previews
- long-horizon multi-view control

## Data Pointer
- `PDF p.1-2` Abstract + Fig. 1：
  - 任务设定、modular fragmentation 问题、`99.2% / 75.4% / 78.0% / 16×` headline，以及 unified architecture 的整体动机都在这里。
- `PDF p.3-4` Fig. 2 + Sec. 3：
  - `Qwen3-VL + frozen DINOv3 + FAST spectral tokenization + compact 1D visual tokenizer` 的主架构、input/output 双侧效率设计都在这里。
- `PDF p.5-6` Table 1：
  - `LIBERO` 主结果在这里；`FUTURE-VLA (w/o HIL)` 与 `FUTURE-VLA (w/ HIL)` 的差异都应回这张表。
- `PDF p.6-7` Table 2：
  - `RoboTwin` 主结果在这里；`75.4%` headline 与 `w/o HIL` / `w/ HIL` 的对比应回这里核定。
- `PDF p.7` Table 3 + real-world result discussion：
  - `Piper` real-world 主结果、`78%` headline，以及 `Table Cleanup` 从 `40%` 到 `64%` 的 HIL 提升都在这里。
- `PDF p.4-5` Fig. 3 + HIL execution section：
  - prediction-guided `HIL`、future previews、interactive execution gating 的运行机制适合后续从这里补证。

## Evidence Links
- [[wiki/evidence/claims/2602_15882_FUTURE-VLA-headline-split.md|2602_15882_FUTURE-VLA-headline-split]]

## 待核点
- `99.2% / 75.4% / 78.0%` 分别来自三个不同 benchmark / platform，后续不能写成一个统一的“平均 headline”。
- `FUTURE-VLA` 的核心结果明显依赖 `HIL` 版本；同时正文也给出 autonomous `w/o HIL` 的 `LIBERO 91.3%` 等结果，后续需要明确默认叙述到底以 `w/ HIL` 还是模型自主能力为中心。
- `16× extended spatiotemporal window while maintaining single-frame latency` 是建立在固定 token budget 压缩设计上的架构级主张，后续若引用到其他部署条件，需要保留实现前提。
- 论文收益同时混合了 unified architecture、input-side compression、latent future prediction、以及 prediction-guided HIL；后续若把增益单独归因于“future forecasting”或“HIL”，需要继续拆分主表与 ablation 证据。
