# 2602_05765_RL-VLA3

## Source
- Raw: [[raw/2602_05765_RL-VLA3.pdf]]
- Extracts manifest: [[extracts/parses/2602_05765_RL-VLA3/manifest.json]]
- Primary text fallback: [[extracts/parses/2602_05765_RL-VLA3/pdftotext.txt]]

## Claim
- 这篇论文要解决的是：现有基于强化学习的 VLA 后训练框架虽然能改善泛化，但训练流水线通常仍然是同步执行的，环境交互、rollout 生成和 actor 更新彼此串行依赖，导致 GPU 长时间空转、吞吐受限，特别是在 simulator 与模型训练争用资源时更明显。
- 核心主张是：如果把 VLA 强化学习训练流水线从 environment interaction、rollout generation 到 actor update 全部改写成分层异步系统，并进一步引入细粒度异步交互与 streaming micro-batch 更新，就能显著提升训练吞吐，同时基本保持训练成功率曲线和最终性能。
- 作者提出 `RL-VLA3`，强调三个层次的异步：`Train Async`、`Rollout Async` 和 `Streamer`。论文 headline claim 是：在 `LIBERO` benchmark 上，相比同步/既有 decoupled 策略，训练吞吐最高提升 `59.25%`；当进一步优化 separation strategy 与 GPU allocation ratio 时，吞吐提升最高可达 `126.67%`；并且从 `8` 到 `256` GPUs 的实验显示该方法在大多数区间具有良好的扩展性。作者同时说明 success-rate curves 与 baseline 基本保持一致，说明效率提升对最终性能影响较小。来源：[[raw/2602_05765_RL-VLA3.pdf]]，第 1 页摘要；第 6-8 页 Table 1、Fig. 2-3；附录 B.1。

## Methodology Index
- RL-VLA3
- reinforcement learning for VLA
- fully asynchronous training pipeline
- full asynchronism
- Train Async
- Rollout Async
- Streamer
- asynchronous environment interaction
- asynchronous rollout generation
- asynchronous actor updates
- disaggregated GPU allocation
- dynamic batching scheduler
- maximum inference batch size
- maximum waiting latency
- micro-batch streaming
- actor-rollout decoupling
- throughput optimization
- scaling law
- RLinf
- LIBERO
- ManiSkill
- OpenVLA-OFT
- π0.5

## Data Pointer
- 摘要与总体命题：[[raw/2602_05765_RL-VLA3.pdf]] 第 1 页摘要与引言。这里给出 `full asynchronism`、`59.25% / 126.67%` throughput headline，以及 environment / rollout / actor 三层异步框架。
- 系统结构与异步机制：[[raw/2602_05765_RL-VLA3.pdf]] 第 3-5 页 Fig. 1 与 Section 3。这里定义 macro-level 与 micro-level asynchronous design、`Dynamic Batching Scheduler`、以及 `Streamer` 如何用 micro-batch 提前启动训练。
- 主吞吐结果：[[raw/2602_05765_RL-VLA3.pdf]] 第 6-7 页 Table 1 与 Fig. 2。这里对应 `LIBERO + π0.5`、`LIBERO + GR00T N1.5`、`ManiSkill + π0` 的 throughput 对比，以及 `59.25%` 与 `126.67%` 的主要出处。
- 扩展性与资源分配：[[raw/2602_05765_RL-VLA3.pdf]] 第 7-8 页 Fig. 3 与相邻正文。这里说明从 `8` 到 `256` GPUs 的 scaling behavior，以及 `1:1 / 2:1 / 3:1` actor-rollout ratio 的 tradeoff。
- 性能保持口径：[[raw/2602_05765_RL-VLA3.pdf]] 附录 B.1 与 Fig. 4。这里说明异步训练下 success rate 曲线与 baseline 趋势相近，支持“提升效率但对最终性能影响较小”的说法。

## 待核点
- 这篇的核心贡献是训练基础设施与系统吞吐，不是推理延迟或 policy architecture 本身；后续如果进入效率 taxonomy，需要避免把它误归为 inference acceleration。
- `59.25%` 与 `126.67%` 都是 throughput 口径，分别对应不同 baseline/placement ratio/async 组合，不能压成单一无条件提升。
- 论文提到 real robot deployments，但正文主结果仍然以 `LIBERO` 与 `ManiSkill` 的 RL training infrastructure 实验为主；“real-world validated”需要比 benchmark throughput claim 更保守。
- success-rate curves 被描述为“similar trend / minimal impact on final performance”，但不是全面优于 baseline 的任务性能论文；后续若写性能结论，需要突出这是效率-性能保持，而不是任务成功率突破。
