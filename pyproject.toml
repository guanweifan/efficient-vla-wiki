[project]
name = "efficient-vla-wiki"
version = "0.1.0"
description = "Local PDF parsing environment for raw-to-wiki workflows."
readme = "AGENTS.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "docling",
    "marker-pdf",
]

[tool.uv]
package = false
